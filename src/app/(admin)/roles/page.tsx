import PlaceholderPage from "@/components/placeholder/PlaceholderPage";

export default function RolesPage() {
  return (
    <PlaceholderPage
      title="Vai trò"
      description="Sprint 12 — sẽ render Role CRUD + dropdown Permission (read-only)."
      badge="Phase A · Sprint 12"
    />
  );
}
