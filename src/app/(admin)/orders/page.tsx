import PlaceholderPage from "@/components/placeholder/PlaceholderPage";

export default function OrdersPage() {
  return (
    <PlaceholderPage
      title="Đơn hàng"
      description="Sprint 7–8 — sẽ render Order List + Order Detail + Status transitions."
      badge="Phase A · Sprint 7-8"
    />
  );
}
