import PlaceholderPage from "@/components/placeholder/PlaceholderPage";

export default function LeadsPage() {
  return (
    <PlaceholderPage
      title="Số cần gọi (Leads)"
      description="Sprint 5 — sẽ render LeadListTable + filter chip theo LeadStatus."
      badge="Phase A · Sprint 5"
    />
  );
}
